import 'package:flutter/material.dart';

class MatchConfigPage extends StatelessWidget {
  const MatchConfigPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Configurações do torneio'),
      ),
    );
  }
}
